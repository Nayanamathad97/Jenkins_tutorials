pipeline {
    agent any

    environment {
        name = "March 26"
    }

    stages {
        stage('STAGE1') {
            steps {
                echo "name: ${env.name}"
                sh 'env'
            }
        }
    }
}