pipeline {
    agent any

    environment {
        name = "nayu"
    }

    stages {
        stage('STAGE1') {
            steps {
                echo "name: ${env.name}"
                sh 'env'
            }
        }
    }
}