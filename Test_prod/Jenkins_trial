pipeline {
    agent any

    parameters {
        string(name:'NAME', defaultValue:'', description:'enter the name')
    }

    environment {
        live_env = 'dev'
    }

    stages {
        stage('STAGE1') {
            when {
                expression {
                    params.NAME == 'Nayu'
                }
            }
            steps {
                echo "===parameter==="
            }
        }
        stage('STAGE2') {
            when {
                environment name: 'live_env', value: 'dev'
            }
            steps {
                echo "==environment=="
                sh 'pwd'
            }
        }
    }
}