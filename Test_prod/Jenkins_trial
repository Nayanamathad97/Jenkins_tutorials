pipeline {
    agent {label 'slave2'}
    environment {
        user = 'Nayu'
        user_id = '123'
    }
    stages {
        stage('STAGE1') {
            steps {
                echo "user : ${env.user}"
                echo "user_id : ${env.user_id}"
                sh '''
                echo user : ${user}
                echo user_id : ${user_id}
                '''
            }
        }
    }
}