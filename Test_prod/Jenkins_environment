pipeline
{ agent any
  environment {
    DOCKER_USER ='nayu'
    AWS_ACCESS_KEY = '827834723672367'
  }
    stages
    {
        stage('STAGE1')
        {
            environment {
                stage = 'stage1'
            }
            steps
            {
                echo "DOCKER_USER: ${env.DOCKER_USER}"
                echo "AWS_ACCESS_KEY: ${env.AWS_ACCESS_KEY}"
                echo "stage: ${env.stage}"

                sh '''
                   env
                '''
            }
        }

        stage('STAGE2')
        { 
            steps
            { 
                echo "DOCKER_USER: ${env.DOCKER_USER}"
                echo "AWS_ACCESS_KEY: ${env.AWS_ACCESS_KEY}"
                echo "stage: ${env.stage}"

            }
        }
    }
}