pipeline{
    <!-- agent {label 'slave1 && slave2'}
    agent {label 'slave1 || slave2'} -->
    agent {label '!master'}
    stages
    {
        stage('STAGE1')
        { 
            steps
            {
                echo "stage one"
            }
        }
    }
}