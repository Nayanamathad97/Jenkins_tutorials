pipeline {
    agent any
    
    options {
        ansiColor('xterm')
        disableConcurrentBuilds(abortPrevious : true)
        buildDiscarder(logRotator(numToKeepStr: '2'))
        disableResume()
        timeout(time: 2, unit: 'MINUTES')
    }

    stages {
        stage('STAGE1') {
            steps {
                echo "\033[31mPreetham \033[35mNayana \033[32m \033[44m" 
                sh 'pwd'
            }
        }
    }
}